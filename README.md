# Setup
```
cd ~/
git clone git@github.com:kcdragon/my-bash-startup.git
echo "source ~/my-bash-startup/.startup.sh" >> ~/.bash_profile # Mac OSX
echo "source ~/my-bash-startup/.startup.sh" >> ~/.bashrc # Ubuntu
```
